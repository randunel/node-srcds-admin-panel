var Log = require('srcds-log');

module.exports = LiveMap;

function LiveMap() {
    ;
}

LiveMap.prototype.getLiveMap = function(req, res) {
    // Render html
};

LiveMap.prototype.sendMessage = function(req, res) {
    //
};
